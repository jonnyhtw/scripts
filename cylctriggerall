#script to poll all suites listed by `cylc scan`

cylc scan > out

while read p 
    do 
        echo 'triggering '${p%% *}
        cylc trigger ${p%% *} *.*:{failed,submit-failed}
    done < out; 

rm out

